# Hanako 功能说明

> 项目当前产品名为 **HanaAgent**，开源仓库名为 **openhanako**。
>
> 本文基于本地源码 `D:\我的项目\openhanako` 整理。审阅版本：`0.421.24`；审阅提交：`e87769a0`。
>
> 上游仓库：[liliMozi/openhanako](https://github.com/liliMozi/openhanako)

## 产品定位

HanaAgent 是一个面向普通用户和开发者的桌面私人 AI Agent。它以 Pi SDK 作为 Agent 运行时，在此基础上增加图形界面、人格、长期记忆、多 Agent、自动化、电脑操作、移动端、外部聊天平台、插件和安全控制等完整产品能力。

## 核心功能

### Agent、人格与记忆

- **独立 Agent** — 可以创建多个 Agent。每个 Agent 都有独立的名称、头像、人格、模型、记忆、Skills、书桌和自动化配置。

- **人格系统** — 通过身份、人设和行为规则文件塑造 Agent 的表达方式与行为逻辑，而不是让所有 Agent 使用同一种默认助手人格。

- **文件夹式 Agent 数据** — Agent 的配置和资料以独立目录保存，便于隔离、迁移和备份。

- **分层记忆系统** — 组合事实数据库、近期记忆、本周记忆、长期记忆、会话摘要和置顶记忆，让 Agent 可以在后续对话中使用过去的信息。

- **记忆检索与整理** — Agent 可以搜索记忆；后台记忆任务会从会话中提取事实、生成摘要，并逐步整理长期内容。

- **记忆开关** — 可以按 Agent 或会话控制是否使用记忆，也可以直接编辑部分记忆事实。

- **角色卡** — Agent 可以导入或导出为本地角色卡 ZIP，按白名单携带人格、头像、可选记忆和 Skills，方便分享或迁移。

- **Skill Bundle** — Skills 可以组成独立技能包，在技能管理页中分组、拖拽、成组启用，并导出为 ZIP。

### 对话与会话

- **桌面聊天界面** — 提供完整的 Electron 图形界面，用于聊天、附件、工具状态、后台活动和结果展示。

- **多模态会话** — 支持文件、图片、视频和音频附件，也支持麦克风录音。模型不具备视觉能力时，可以通过辅助视觉模型先理解图片。

- **会话管理** — 支持搜索聊天记录、继续旧会话、归档、恢复和永久删除；标题优先匹配，必要时可继续搜索正文。

- **会话上下文压缩** — 长对话可以压缩旧上下文；无法继续的历史会话可以保留为只读记录，再从主 Agent 创建新会话继续。

- **文本引用追问** — 可以选中聊天正文，将原文作为引用卡片带入输入框继续提问。

- **项目与工作目录** — 每个会话可以选择工作台、项目目录和额外授权目录，Agent 会在相应范围内访问文件。

- **会话权限模式** — 支持自动审核、完整权限、操作前询问和只读模式，控制 Agent 是否可以执行具有副作用的工具。

- **思考强度** — 可以按模型能力设置关闭、自动、中等、深度或更高等级的推理强度。

### 多 Agent 协作

- **Subagent 委派** — 主 Agent 可以把任务交给子 Agent 后台执行，并选择只读或可操作权限；任务完成后结果会自动返回原会话。

- **可继续的子 Agent 实例** — Subagent 会保留独立的会话身份，可以查看执行过程，并在同一实例上继续派发后续任务。

- **Workflow 编排** — 支持用工作流组织多个 Agent 节点，进行串行或并行协作；后台运行完成后将结果送回对话。

- **工作流断点续跑** — 可以根据已有运行 ID 复用未变化的已完成节点，从发生变化的位置继续执行。

- **Agent 频道群聊** — 多个 Agent 可以加入内部频道，读取群聊上下文、回复消息或决定本轮不发言。

- **Agent 私信** — Agent 之间可以通过 DM 进行一对一通信和任务交流。

- **独立记忆边界** — 各 Agent 的人格和记忆相互隔离；频道记忆也有会话作用域，跨频道搜索需要显式请求。

## 工具能力

### 文件、终端与办公

- **文件操作** — 支持读取、创建、修改、移动和删除文件，并可将生成文件登记为当前会话的输出文件。

- **一次性命令** — 可以执行 Shell 命令、脚本和代码，用于开发、数据处理及日常电脑任务。

- **持续终端会话** — 可以启动并保持一个长期运行的终端进程，继续发送输入、获取输出或终止任务。

- **文件搜索** — 支持按文件名和内容搜索项目，可调用本地搜索工具处理较大的目录。

- **Office 文档读取** — 内置工具可读取 `.docx`、`.xlsx`、`.xlsm`、`.pdf`、`.txt`、`.md`、`.csv`、`.tsv` 和 `.html`。

- **HTML 导出 PDF** — 可以利用桌面 Chromium 打印引擎，把静态 HTML 导出为 PDF 会话文件。

- **文件修改备份** — 可在 AI 修改或删除文件前自动保存原始内容，并在设置中查看和恢复备份。

### 网络、浏览器与电脑操作

- **网页搜索** — 可以通过搜索 API 或本机浏览器搜索互联网，并将结果整理给 Agent。

- **网页读取与检查** — 支持抓取网页、提取正文、检查页面结构和读取当前浏览器内容。

- **浏览器自动化** — 可以打开网页、点击、输入、滚动、截图，并完成需要真实页面交互的任务。

- **持续浏览器会话** — 浏览器可以在后台保持运行，用户能够打开查看当前状态。

- **Computer Use** — 可在用户开启后控制桌面应用；当目标应用无法后台操作时，可以切换为前台接管，并允许用户按 `Esc` 中止。

- **截图能力** — 支持普通截图、分段长截图，以及聊天内容的移动端或桌面端排版截图。

- **出站代理** — 应用 API、OAuth、Bridge 和网页请求可以跟随系统代理、使用手动 HTTP/HTTPS/SOCKS 代理或直连。

### 媒体与内容生成

- **图片理解** — 支持视觉模型；文本模型无法读取图片时，可以启用 Vision Bridge，由辅助视觉模型生成图片描述。

- **语音识别** — 可以配置语音识别 Provider，将录音或外部平台收到的语音消息转成文字。

- **图片生成** — Agent 可以异步提交文生图或图生图任务，支持多张参考图、数量、比例、分辨率、质量、模型和 Provider 等参数。

- **视频生成** — Agent 可以异步提交视频生成任务，完成后自动把结果送回当前会话。

- **全屏媒体查看器** — 聊天和书桌中的图片、SVG、视频可以全屏预览，支持缩放、平移和浏览相邻媒体。

- **内容卡片与 HTML 预览** — 工具和插件可以返回卡片、HTML 页面、文件、截图或媒体结果，并在聊天记录中统一展示和回放。

### 日常协助

- **Todo 管理** — Agent 可以创建和更新任务清单，并在界面中展示当前任务状态。

- **通知** — Agent、定时任务和插件可以发送桌面通知；符合条件的通知也能回发到外部聊天平台。

- **设置管理** — Agent 可以搜索并修改允许开放的应用设置，例如模型、工作目录、记忆、主题和安全相关选项。

- **经验库** — 除长期记忆外，还提供可开关的经验能力，用于积累可复用的工作经验。

## 书桌与自动化

- **Agent 书桌** — 每个 Agent 都有独立书桌，可放置文件和笺，作为用户与 Agent 之间的异步协作空间。

- **文件拖拽与预览** — 书桌支持拖入文件、查看目录、预览媒体，并监听工作台文件树变化。

- **笺驱动任务** — 用户可以在书桌留下类似便签的笺，Agent 在巡检时读取并根据内容采取行动。

- **心跳巡检** — Agent 可以按指定间隔检查书桌和工作环境，在没有即时对话的情况下主动处理变化。

- **Cron 定时任务** — 支持创建、启用、暂停和执行定时任务，并保存运行记录。

- **多种自动化执行器** — 复杂任务可以启动后台 Agent Run；轻量任务可以直接发送提醒；插件工具也可以作为计划动作执行。

- **自动化权限控制** — 后台任务同样支持自动审核、完整权限和只读模式，避免定时任务默认获得不受限制的操作能力。

- **后台活动记录** — 心跳、定时任务、Subagent、Workflow 和媒体生成等活动会显示状态、历史和产生的文件。

## 外部平台与远程访问

- **Telegram 接入** — 可以把指定 Agent 接入 Telegram Bot，远程聊天和使用 Agent 能力。

- **飞书与 Lark 接入** — 支持飞书中国和 Lark 海外区域，通过长连接接收事件并发送富文本或流式回复。

- **钉钉接入** — 支持钉钉企业内部应用和机器人，通过 Stream 长连接收发消息。

- **QQ 机器人接入** — 支持 QQ Bot，并能通过官方上传接口发送文件和富媒体。

- **微信接入** — 支持通过微信 iLink 协议扫码绑定个人账号；一个微信号同时只绑定一个 Agent。

- **Owner 身份控制** — 外部平台可以指定哪个账号是用户本人，Owner 才拥有完整的记忆、人格和工具权限。

- **Bridge 权限模式** — 外部聊天平台可设为自动审核、完整权限或只读模式，并支持回复前回执和富文本流式输出。

- **移动端 PWA** — HanaAgent Server 可以托管 `/mobile/` 页面，手机通过设备访问密钥或本地账号登录，查看会话、继续聊天和管理工作台文件。

- **局域网桌面连接** — 另一台桌面端可以通过 LAN URL 和 Access Key 连接到已有 HanaAgent Server，共享同一套 Agent、会话和资源。

- **Server-first CLI** — 命令行客户端可以连接正在运行的 HanaAgent Server，查看状态、列出会话并继续对话。

## 模型与 Provider

- **多 Provider 接入** — 支持 OpenAI 兼容接口、Anthropic 风格接口、OAuth Provider、Ollama 本地模型和自定义 Base URL。

- **自定义模型** — 可以添加 Provider、拉取或手动配置模型，并设置上下文、视觉、推理和协议兼容信息。

- **多角色模型配置** — 首次设置会区分对话模型、小工具模型和大工具模型；还可以单独指定辅助视觉、语音识别和媒体生成模型。

- **OAuth 登录** — 当前界面支持 OpenAI OAuth；代码层也提供通用 OAuth 流程和 Provider 扩展接口。

- **用量展示** — 可以记录并展示模型使用量，但最终计费仍以模型供应商为准。

## Skills、MCP 与插件

### Skills

- **社区 Skills 兼容** — 支持以 `SKILL.md` 为核心的技能目录，可以安装和启用社区技能。

- **主动学习技能** — 在用户允许时，Agent 可以从 GitHub 搜索、安装 Skills，也可以编写新的 Skill。

- **Skill 安全审核** — 默认对安装的 Skills 进行审核；用户可以在设置中调整相关策略。

### MCP 连接器

- **内置 MCP 连接器插件** — 项目随附 `mcp` 系统插件，可把 MCP Server 的工具暴露给 Agent。

- **本地 MCP** — 支持启动本地 `stdio` MCP Server，并配置命令、参数、工作目录和专用环境变量。

- **远程 MCP** — 支持 Streamable HTTP 和 SSE，可使用无认证、Bearer Token、自定义 Header 或 OAuth。

- **配置导入** — 可以导入 Cherry Studio、Claude 等应用使用的 `mcpServers` JSON 配置。

- **按 Agent 启用** — MCP 总开关之外，每个连接器还能像 Skill 一样只对选中的 Agent 开放。

### 插件系统

- **拖拽安装** — 社区插件可以通过文件夹或 ZIP 拖入设置页安装，支持启用、禁用、更新和删除，通常无需重启。

- **多种贡献类型** — 插件可以提供工具、Skills、斜杠命令、Agent 模板、设置 Schema、HTTP 路由、LLM/媒体 Provider、Pi SDK Extension、页面、侧栏 Widget、消息卡片和后台任务。

- **插件市场** — 设置页可以浏览可安装插件，查看 README、版本、兼容性、权限和贡献类型。

- **两级插件权限** — Restricted 插件使用受限扩展接口；Full-access 插件可以注册路由、Provider、运行时工具和生命周期钩子，但必须由用户开启全权插件权限。

- **插件独立数据目录** — 插件配置与数据存放在自己的目录中；卸载插件时可以保留数据，重新安装后继续使用。

- **插件开发工具** — 提供安装开发副本、热重载、启停、重置、卸载、工具调用、诊断、页面检查和场景测试等开发流程。

## 安全与数据

- **PathGuard 文件访问控制** — 应用层会区分工作目录、受控数据目录、显式授权目录和普通外部路径，限制写入和删除范围。

- **操作系统级沙盒** — macOS 使用 Seatbelt，Linux 使用 Bubblewrap，Windows 使用 restricted token 和专用执行辅助程序。

- **工具调用审批** — 会话处于“操作前询问”模式时，修改本地或外部状态的操作会显示确认界面。

- **自动审核** — 自动模式会根据工具声明和副作用进行审核，不通过的操作会被拒绝。

- **额外目录授权** — Agent 请求修改会话额外目录时，需要用户确认；授权不会自动改变当前工作目录。

- **安全审计记录** — 核心提供安全审计日志，用于记录相关权限和安全事件。

- **本地优先存储** — 默认数据目录由 `HANA_HOME` 决定；生产环境默认位于 `~/.hanako`，开发环境默认位于 `~/.hanako-dev`。

- **独立 Server 架构** — Node.js/Hono Server 作为独立进程运行，Electron 前端通过 WebSocket 与其通信；会话、资源、Bridge 和移动端复用同一个 Server。

## 平台与界面

- **Windows、macOS、Linux** — macOS Apple Silicon、macOS Intel 和 Linux 已支持；Windows 当前标记为 Beta。

- **多语言界面** — 支持简体中文、英文、日文、韩文和繁体中文。

- **主题与显示设置** — 支持外观主题、字体、截图主题和不同屏幕布局。

- **首次启动向导** — 引导用户设置语言、用户名称、Agent 名称、Provider 和不同用途的模型。

## 当前实现边界

- **Windows 仍是 Beta** — Windows 版本可以运行，但官方尚未把它标为完全稳定状态。

- **Windows 沙盒不隔离网络** — Windows 命令沙盒主要限制文件写入；读取和联网仍按当前 Windows 用户权限执行。

- **关闭沙盒风险较高** — 关闭沙盒后，Agent 执行的普通进程可能读写或删除当前用户有权限访问的文件。

- **插件权限不是代码级沙盒** — Restricted 和 Full-access 区分的是插件可使用的 Hana 扩展接口；社区插件代码仍在主进程运行，安装前需要确认来源。

- **Office 能力有格式限制** — 内置 Office 工具不支持 `.ppt`/`.pptx` 读取，也不直接编辑 Word、Excel、PowerPoint 或 PDF；扫描版 PDF 暂无 OCR。

- **移动端依赖同一 Server** — 当前移动端是 HanaAgent Server 托管的 PWA，不是独立云端 Agent 服务。

- **WhatsApp 尚未开放** — 界面中标记为“即将支持”，当前不能算作正式接入能力。

- **外部服务需要单独配置** — 模型、搜索、语音识别、媒体生成、MCP 和聊天平台通常需要相应 API、账号、凭证或本地服务。

- **记忆效果仍有优化空间** — 记忆功能已经完整落地，但实际提取质量会受到模型能力、对话内容和记忆配置影响，项目 README 也明确说明仍需继续优化。

## 技术架构概览

```text
Electron + React 桌面端
          │
          ▼
Hono HTTP / WebSocket Server
          │
          ▼
Engine + Manager + Hub 调度层
          │
          ├── Pi SDK Agent 运行时
          ├── 记忆 / Skills / 工具 / 沙盒
          ├── 多 Agent / 频道 / 自动化
          ├── Bridge / Mobile / CLI
          └── Plugin Runtime / MCP / Provider
```

主要技术包括 Electron 42、React 19、Zustand 5、Vite 7、Hono、Pi SDK、better-sqlite3 和 Vitest。
