# 建议 Pi200 开发功能

> 本文综合以下四份功能说明形成：
>
> - `Pi功能说明.md`
> - `hanako功能说明.md`
> - `Hermes功能说明.md`
> - `Openclaw功能说明.md`
>
> 建议基于当前事实：Pi200 以 Pi 为底层，未来需要桌面端，允许修改 Agent 流程，并希望作为公开 GitHub 项目长期维护。

## 结论

建议把 Pi200 定位为：

> **一个以 Pi 为可靠 Agent 内核、Windows 优先、本地优先、拥有独立人格与可控记忆，并能逐步扩展到自动化和多 Agent 的私人桌面 Agent。**

Pi200 不应该在第一阶段复制 Hanako、Hermes 或 OpenClaw 的全部功能。最有价值的差异化不是“渠道最多”或“工具最多”，而是：

1. 保留 Pi 简洁、稳定、易于修改的 Agent Loop。
2. 提供普通用户可以理解的桌面界面。
3. 先建立可靠的权限、数据和审计边界。
4. 让人格和记忆可见、可编辑、可关闭、可迁移。
5. 所有后台行动必须可追踪、可停止、可恢复。
6. 通过扩展接口增加能力，不持续膨胀核心工具 Schema。

## 四项目对比

| 能力 | Pi | Hanako | Hermes Agent | OpenClaw |
| --- | --- | --- | --- | --- |
| 核心定位 | 极简终端 Coding Agent | 桌面私人 Agent | 自学习、跨环境 Agent | 常驻多渠道 Agent 平台 |
| 终端交互 | 强 | 有 CLI 接口 | 强，完整 TUI | 强，CLI/TUI 完整 |
| 桌面 GUI | 无 | 完整 Electron 应用 | 完整 Electron 应用 | macOS/Windows/Linux Hub 与 Web UI |
| Agent Loop 可修改性 | 强，源码直接可控 | 已在产品层深度改造 | Python 核心较大 | Gateway 与插件体系复杂 |
| 模型与自定义 Provider | 强 | 强 | 强 | 很强，Provider 数量最多 |
| 会话保存与分支 | 强 | 强 | 强 | 强 |
| 人格系统 | 仅提示词定制 | 完整、多 Agent 隔离 | SOUL + Personality + Profile | SOUL/IDENTITY/USER + 多 Agent |
| 长期记忆 | 无内置 | 分层记忆与事实库 | 有界记忆 + FTS5 + 外部 Provider | 文件记忆 + 多种插件后端 |
| Skills | 原生支持 | 支持并可主动安装 | 强调自动创建和改进 | Skills + ClawHub 生态 |
| 插件/扩展 | Extension、Package | 工具、页面、Provider、后台任务 | Python 插件和 Hooks | 插件 SDK、ClawHub、官方插件最多 |
| MCP | 无内置 | 内置连接器插件 | MCP Client/Server | MCP Client/Server，管理和诊断完整 |
| 权限审批 | 无逐工具审批 | 会话权限 + 自动审核 | 命令与写入审批 | 五档 Exec 策略 + 多层门控 |
| 内置沙盒 | 无 | PathGuard + OS 沙盒 | 容器/远程环境隔离 | Docker 沙盒 + Host 审批 |
| 浏览器/电脑操作 | 无内置 | 有 | 有 | 有，且可通过设备节点操作 |
| Office 与文件预览 | 基础文件读取 | 桌面预览 + Office 工具 | Skills/工具扩展 | PDF、文档和插件扩展 |
| 图片/音频/视频 | 图片输入 | 理解、录音、生成、预览 | 理解、生成、STT/TTS | 理解、生成、STT/TTS、音乐 |
| Cron/Heartbeat | 无 | 两者都有 | Cron + 后台复盘 | Cron + Heartbeat + Hooks |
| Subagent | 无内置 | 有 | 有 | 有 |
| 多 Agent | 无内置 | Profile、频道、DM、Workflow | Profile、Subagent、Kanban | 路由、账号绑定、Subagent、ACP |
| 持久任务板 | 无 | 无独立 Kanban | SQLite Kanban | Workboard 插件 |
| 消息平台 | 无 | 中等数量，偏中国平台 | 很多，含中国与海外平台 | 最多，渠道插件体系成熟 |
| 移动端 | 无 | 同 Server PWA | 消息平台/Termux | iOS、Android、watchOS 节点 |
| 远程执行 | 依靠外部环境 | 以本机为主 | 本机/Docker/SSH/云环境 | Gateway/Node/Docker/远程连接 |
| 自动学习 | 无内置 | 可主动安装/编写 Skill | 最强，闭环复盘与 Skill 改进 | 有可选自学习与记忆插件 |
| 研究轨迹 | JSON 事件可外部处理 | 非重点 | 批量轨迹与压缩 | 可观测性强，训练轨迹非核心 |
| 复杂度 | 最低 | 中高 | 高 | 最高 |

## 应该保留的 Pi 优势

### 1. 保持 Pi 内核可跟随上游

Pi200 已经 Fork 官方 Pi，今后应继续保留 `upstream`，定期合并上游 Bug 修复。不要为了产品功能随意改动所有 Pi Packages。

推荐原则：

- Agent 核心确实无法扩展时，才修改 Pi 原有源码。
- 人格、记忆、权限、自动化和桌面通信优先放在 `packages/Pi200`。
- 桌面界面只放在 `apps/desktop`。
- 对 Pi 核心的修改保持小而清晰，并给每项修改补回归测试。
- 记录每个上游冲突的原因，避免未来无法合并更新。

### 2. 继续使用 Pi 的现有能力

以下能力不需要在 Pi200 中重新实现：

- 多模型和自定义 Provider。
- `read`、`bash`、`edit`、`write`、`grep`、`find`、`ls`。
- 图片输入和文件引用。
- 会话保存、恢复、分叉、克隆和树状历史。
- 上下文压缩与分支总结。
- `AGENTS.md`、系统提示词和 Prompt Templates。
- Agent Skills、Themes、Pi Packages。
- SDK、RPC 和 JSON 事件流。
- Extension 的自定义工具、命令、TUI 和事件拦截能力。

## Pi200 必须先做的基础

### 1. 独立产品身份与数据目录

Pi200 不应长期直接复用 `~/.pi/agent` 作为自己的全部数据目录。否则原版 Pi 和 Pi200 会共享设置、会话、扩展与凭证，后续修改数据结构时容易互相影响。

建议：

- 产品 ID：`pi200`
- 显示名称：`Pi200`
- 默认用户数据目录：Windows `%USERPROFILE%\.pi200`
- 支持环境变量：`PI200_HOME`
- Provider 凭证、会话、记忆、日志和插件数据分目录存放。
- 首次运行可以选择“从 Pi 导入设置”，但不要默认持续共用同一份文件。
- 敏感凭证永远不进入 Git 仓库。

### 2. 权限与审计系统

权限必须早于 Cron、Heartbeat、电脑操作和多 Agent。没有安全边界就增加后台能力，会让风险和调试成本同时失控。

建议提供四档会话权限：

| 模式 | 行为 |
| --- | --- |
| 只读 | 只允许读取、搜索和分析，不允许写文件或执行有副作用命令 |
| 操作前询问 | 读取自动执行，写入、删除、命令和外部操作逐次确认 |
| 自动审核 | 低风险操作自动通过，高风险或无法判断时询问用户 |
| 完整权限 | 可信环境中执行所有已启用工具，不逐次询问 |

同时实现：

- 工具声明读写范围和副作用类型。
- 命令与文件操作使用同一权限判定入口。
- 工作目录外默认只读，额外写入目录由用户显式授权。
- 每次拒绝都向 Agent 返回清晰原因和下一步，而不是静默失败。
- 保存安全审计日志：谁、何时、通过哪个会话、调用什么工具、结果如何。
- 支持立即停止当前任务和所有子任务。
- 后台自动化使用独立权限策略，不能继承交互会话的临时完整权限。

### 3. 稳定的数据模型

建议从一开始使用 SQLite 保存结构化状态：

- Agent/Profile 元数据。
- 会话索引与桌面搜索数据。
- 记忆事实和来源。
- 工具审批与安全审计。
- Cron、Heartbeat 和后台运行记录。
- Subagent、Workflow 和任务状态。

Markdown 只用于用户需要直接阅读、编辑、导入或导出的内容，例如人格、项目规则和记忆摘要。运行状态不要散落成大量 JSON 文件。

## 第一阶段：形成可用的 Pi200

第一阶段目标不是功能最多，而是让用户愿意每天打开 Pi200。

### 1. 桌面聊天壳

建议优先实现：

- Windows 优先的桌面启动与自动更新框架。
- 会话列表、搜索、创建、重命名、归档和删除。
- 流式文本、思考状态、工具调用和错误展示。
- 输入框、附件、图片、文件引用和拖拽。
- 当前模型、推理强度、工作目录和权限模式快速切换。
- 中止、重试、继续、分叉和查看会话树。
- 文件 Diff 和工具结果的侧边预览。
- Provider 与模型配置页面。
- 诊断页：版本、数据目录、代理、模型、Git 和工具环境。

桌面端通过 Pi SDK/RPC 调用 Agent，不在 React 中复制 Agent Loop。

### 2. Agent Profile 与人格

每个 Agent 建议拥有：

- `identity.md`：名称、身份和公开描述。
- `soul.md`：性格、价值观、表达方式和行为边界。
- `user.md`：该 Agent 理解的用户资料。
- `instructions.md`：长期工作规则。
- 头像、主题色和默认模型。
- 独立记忆、会话和工作目录设置。

第一阶段先支持一个主 Agent，但数据结构从一开始使用 `agentId`，避免未来增加多 Agent 时迁移全部数据。

### 3. 可控的基础记忆

建议结合 Hermes 的“有界记忆”和 Hanako 的“分层记忆”，但第一版保持简单：

- 置顶事实：始终进入 Prompt，容量严格受限。
- 用户资料：姓名、偏好、沟通方式，可直接编辑。
- 会话摘要：会话结束或压缩时生成，不必每轮更新。
- 历史检索：先用 SQLite FTS5 搜索真实会话，不急于引入向量数据库。
- 记忆来源：每条记忆保存来自哪个会话和消息。
- 写入审批：关闭、通知、审批三种策略。
- 记忆管理页：搜索、编辑、删除、固定和查看来源。
- Agent 明确删除的记忆应真正从后续 Prompt 和检索中移除。

第一版不要自动修改 Skills。先把“保存事实”和“修改能力”分开，避免 Agent 在用户不知情时改变行为。

### 4. 文件与工作区体验

- 每个会话有一个主工作目录。
- 可以临时授权额外只读或可写目录。
- 桌面端显示文件树和最近变更。
- AI 修改前保存短期备份。
- 支持 Diff 审核、接受、恢复。
- 输出文件登记到会话，避免结果只存在一段路径文字中。
- 图片、文本、Markdown、HTML 和 PDF 提供可靠预览。

## 第二阶段：成为日常私人助理

### 1. Skills 管理

- 继续兼容 Pi Agent Skills。
- 增加图形化安装、启停、来源、版本和依赖状态。
- 安装前扫描脚本、网络请求、危险命令和敏感路径。
- 区分内置、用户、本项目和第三方 Skills。
- 支持导入、导出和 Skill Bundle。
- Agent 可以建议创建或修改 Skill，但默认必须由用户查看 Diff 后批准。

### 2. MCP Client

建议在核心稳定后加入：

- 本地 `stdio`、SSE 和 Streamable HTTP。
- Bearer、Header 与 OAuth。
- MCP Server 状态、工具、资源和 Prompt 检查。
- 按 Agent 启用和工具 Include/Exclude。
- 真实连接测试，而不是保存配置就显示成功。
- MCP 工具必须继续经过 Pi200 权限系统。

MCP Server 模式可以更晚开发，除非桌面端或外部自动化明确需要反向调用 Pi200。

### 3. 浏览器与 Office

建议顺序：

1. 网页搜索和正文提取。
2. 独立浏览器 Profile 的导航、点击、输入和截图。
3. 用户主动连接现有浏览器标签页。
4. PDF、DOCX、XLSX、CSV 的只读解析。
5. HTML 导出 PDF。
6. 扫描 PDF OCR 和 Office 编辑作为插件，不进入初始核心。

浏览器工具必须防 SSRF，下载文件必须进入受控目录，使用用户现有登录态前必须显式提示。

### 4. Cron 与 Heartbeat

- Cron 支持一次性、固定间隔和 Cron 表达式。
- Heartbeat 只读取明确的巡检清单，不允许无限自由行动。
- 后台任务有独立会话、权限、预算、超时、重试和停止按钮。
- 每次执行显示开始、进行中、成功、失败或跳过原因。
- 轻量提醒不启动完整 Agent，以降低费用。
- 后台任务完成后可发送桌面通知。
- 默认关闭主动修改文件；用户明确启用后才允许。

### 5. 基础语音和多媒体

- 桌面录音和语音转写。
- 可选 TTS 回复。
- 图片理解和全屏预览。
- 视频只先做附件与预览，不急于实现复杂视频理解和生成。
- 图片/视频生成作为 Provider 插件，不固定在核心。

## 第三阶段：多 Agent 与远程使用

### 1. Subagent

优先实现临时 Subagent，而不是一开始做复杂多 Agent 社交系统：

- 主 Agent 派发独立任务。
- 只读与可写两档权限。
- 并发数、递归深度、Token 和时间预算。
- 独立会话和可查看执行过程。
- 结果与输出文件自动回到父会话。
- 父任务停止时传播中断。
- 任务失败必须返回明确原因和可重试入口。

### 2. Workflow

Subagent 稳定后，再增加声明式 Workflow：

- 串行、并行和依赖节点。
- 每个节点指定 Agent、模型、Skill、权限和预算。
- 节点结果结构化传递。
- 失败重试和从失败节点继续。
- 用户审批节点。
- 可视化运行记录。

持久 Kanban 应在 Workflow 之后开发。只有出现跨天、跨角色、跨重启协作需求时，才值得引入任务状态机、租约、回收和 Worker 调度。

### 3. 消息 Gateway

不要第一版支持十几个平台。建议先选：

1. Telegram：开发和测试成熟，远程个人使用方便。
2. 飞书：适合中文办公和文件场景。

Gateway 基础能力包括：

- Owner 配对与允许名单。
- 每个平台独立权限模式。
- 文本、图片、文件和语音附件。
- 长回复分块与失败重试。
- 会话路由和来源标识。
- 后台任务通知回发原平台。
- 一个账号只能由一个运行实例持有，避免重复连接。

QQ、微信、钉钉、Discord、Slack 和 WhatsApp 应通过插件逐步增加。

### 4. 移动端

第一步建议做与同一 Pi200 Server 相连的响应式 Web/PWA，而不是原生 iOS/Android：

- 查看和继续会话。
- 上传文件、图片和录音。
- 查看后台任务。
- 停止 Agent。
- 管理工作台输出文件。
- 设备 Access Key 与可撤销会话。

相机、位置、系统通知和电脑控制等 Node 能力，应在 PWA 稳定后再评估。

## 第四阶段：生态与高级能力

### 1. 插件 SDK

在出现至少三类真实扩展需求后，再稳定公开插件 API。建议支持：

- Tools、Skills、Commands。
- Provider 与认证。
- 消息渠道 Adapter。
- Memory Provider。
- 后台任务执行器。
- 桌面页面、侧栏和消息卡片。
- Agent Loop 事件拦截。
- 插件独立数据目录。
- 版本兼容和最小 Pi200 版本。

权限至少分为：声明式受限插件和完整运行时插件。必须明确说明这属于扩展接口权限，不等同于代码沙盒。

### 2. 高级记忆与自学习

在基础记忆准确、可解释之后，再增加：

- 向量检索和混合搜索。
- 时间衰减与事实冲突检测。
- 记忆合并、过期和来源可信度。
- 长期用户模型。
- 后台复盘。
- Skill 创建与改进建议。

默认策略应是“建议并展示 Diff”，而不是静默自我修改。真正的全自动学习只作为高级开关。

### 3. 远程执行环境

可以参考 Hermes，按需求增加：

- Docker 执行环境。
- SSH 远程主机。
- 可暂停的云开发环境。
- 每个 Agent 的独立 Worktree。

早期不建议同时支持 Daytona、Modal、Singularity 等所有后端。先定义统一执行接口，再实现本机和 Docker 两个后端。

### 4. 设备节点

OpenClaw 的 Node 架构适合长期目标，但实现成本很高。只有当 Pi200 已有稳定 Gateway 和权限协议后，再开发：

- 设备身份与配对。
- 能力声明与逐项授权。
- 屏幕、相机、通知和位置。
- Windows 电脑控制节点。
- 节点版本兼容协议。

## 不建议早期开发的功能

- 同时接入所有消息平台。
- 默认开启的自动自我修改。
- 没有审计记录的 Heartbeat。
- 无权限边界的 Computer Use。
- 自建复杂向量数据库集群。
- 原生 iOS、Android 和 watchOS 三端同时开发。
- 大规模 Swarm。
- 持久 Kanban、Workflow 和 Subagent 同时开工。
- 自建插件市场和支付系统。
- 同时实现大量图片、视频、音乐 Provider。
- 为了兼容未知未来需求提前设计庞大插件 API。
- 在桌面前端重新实现一套 Agent Loop。

## 推荐开发优先级

| 优先级 | 功能 | 用户价值 | 技术复杂度 | 风险 |
| --- | --- | --- | --- | --- |
| P0 | 独立数据目录与配置迁移 | 高 | 中 | 数据兼容 |
| P0 | 权限、审批、停止和审计 | 极高 | 高 | 安全核心 |
| P0 | Pi 上游同步与回归测试 | 极高 | 中 | 长期维护 |
| P1 | 桌面聊天、会话和工具展示 | 极高 | 高 | 跨进程状态 |
| P1 | Agent 人格/Profile | 高 | 中 | Prompt 一致性 |
| P1 | 有界记忆 + 会话 FTS5 | 高 | 中 | 错误记忆 |
| P1 | 工作区、Diff 和文件备份 | 高 | 中 | 文件安全 |
| P2 | Skills 管理和审核 | 高 | 中 | 供应链安全 |
| P2 | MCP Client | 高 | 中 | 第三方工具权限 |
| P2 | 浏览器与 Office 只读能力 | 高 | 高 | 登录态与恶意内容 |
| P2 | Cron、Heartbeat、通知 | 高 | 高 | 后台副作用 |
| P2 | 录音、STT、TTS | 中 | 中 | Provider 与隐私 |
| P3 | Subagent | 高 | 高 | 成本与并发 |
| P3 | Workflow | 中高 | 高 | 状态恢复 |
| P3 | Telegram、飞书 Gateway | 中高 | 高 | 身份与路由 |
| P3 | 移动 PWA | 中 | 高 | 远程认证 |
| P4 | 插件 SDK | 高 | 极高 | API 长期兼容 |
| P4 | 高级记忆与自动学习 | 中高 | 极高 | 行为漂移 |
| P4 | Docker/SSH 执行后端 | 中高 | 高 | 隔离与凭证 |
| P4 | 原生设备节点 | 中 | 极高 | 平台权限与协议 |

## 推荐目录边界

现阶段可以沿用并逐步扩展：

```text
Pi200/
├── apps/
│   └── desktop/                 # Electron/桌面界面，只负责用户体验
├── packages/
│   └── Pi200/                   # Pi200 产品逻辑
│       ├── src/
│       │   ├── config/          # 产品配置和迁移
│       │   ├── identity/        # Agent/Profile/人格
│       │   ├── memory/          # 记忆、摘要、检索
│       │   ├── permissions/     # 权限、审批、审计
│       │   ├── storage/         # SQLite 和数据模型
│       │   ├── tools/           # Pi200 专属工具
│       │   ├── automation/      # Cron、Heartbeat、通知
│       │   ├── workflows/       # Subagent 与 Workflow
│       │   └── gateway/         # 后续消息与移动端服务
│       └── test/
├── scripts/                     # 开发、启动、同步上游和发布脚本
└── Pi 原有 packages/            # 尽量保持与上游一致
```

当 Gateway、插件 SDK 或移动端真正变大时，再拆成独立 Package。当前不要为了“看起来完整”创建大量空目录。

## 推荐的首个可交付版本

Pi200 第一个真正可供他人使用的公开版本，建议只承诺以下内容：

1. Windows 桌面聊天。
2. 自定义 OpenAI 兼容 Provider 和模型切换。
3. Pi 完整文件、命令、搜索和会话能力。
4. 四档权限与操作审批。
5. 一个可定制人格的主 Agent。
6. 可查看、可编辑、可关闭的基础记忆。
7. 会话搜索、归档、恢复、分叉和压缩。
8. 工作区文件树、Diff、备份和输出文件预览。
9. Skills 查看、启停和安全提示。
10. 清晰的安装、更新、诊断和数据目录说明。

这十项完成后，Pi200 才拥有独立产品价值。Cron、MCP、浏览器、多 Agent、消息渠道和移动端都可以在此基础上按真实使用需求增加。

## 最重要的工程原则

- **上游优先** — Pi 已经解决的问题不重写。
- **安全先于自主** — 先有权限和审计，再有后台行动。
- **数据可迁移** — 人格、记忆、会话和配置必须可备份、导入和导出。
- **失败必须可见** — 每次操作都要有成功、失败、拒绝、跳过或取消状态。
- **Agent 不得暗中改变自己** — 自动修改记忆、人格、Skills 和配置必须遵守用户策略。
- **核心工具保持精简** — 专用能力通过 Skill、Extension、MCP 或按条件加载，减少每轮 Tool Schema 成本。
- **桌面只是客户端** — Agent、权限、存储和自动化逻辑放在产品核心，不散落到 UI。
- **先服务单用户** — 多租户、团队权限和企业管理不应挤占早期开发资源。
- **Windows 优先但协议跨平台** — 第一阶段把 Windows 做稳定，同时避免把核心能力写死为 Windows API。
- **每阶段都有可运行产品** — 不同时铺开桌面、网关、移动端和多 Agent，避免长期只有骨架没有完成体验。
