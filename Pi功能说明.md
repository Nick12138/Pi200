# Pi 功能说明

> 基于 Pi `0.82.1` 整理。本文只描述 Pi 当前已经具备的能力，不包含 Pi200 规划但尚未实现的功能。

## 功能特性

- **终端编程 Agent** — 在命令行中与 AI 交互，可以阅读项目、分析代码、修改文件、执行命令并协助完成开发任务。

- **完整的代码操作工具** — 内置 `read`、`bash`、`edit`、`write`、`grep`、`find`、`ls` 等工具，能够搜索项目、查看源码、编辑文件和运行程序。

- **多模型与多供应商支持** — 支持 OpenAI、Anthropic、Google 等模型供应商，也可以配置自定义 API 地址、密钥、模型名称和上下文参数。

- **运行时模型切换** — 可以在会话过程中切换模型，并设置可用模型范围、默认模型和推理强度。

- **多模态输入** — 支持向模型提供图片，并可在提示词中引用项目文件，适合分析截图、界面和图像资源。

- **Shell 命令集成** — 可以直接执行终端命令，也支持用户在交互界面中手动运行 Shell 命令并将结果带入上下文。

- **会话自动保存** — 对话和工具调用会自动记录，可以恢复之前的会话，继续未完成的开发工作。

- **树状会话与分支** — 会话记录采用树状结构，可以查看历史节点、从任意位置创建分支，或者克隆现有会话进行不同方案的尝试。

- **上下文压缩** — 当对话接近模型上下文上限时，可以压缩早期内容，并为被省略的会话分支生成摘要。

- **项目级指令** — 支持读取 `AGENTS.md`、`CLAUDE.md` 等项目说明文件，用于定义开发规范、项目背景和 Agent 工作规则。

- **系统提示词定制** — 可以替换或扩展默认系统提示词，让 Agent 按照指定身份、流程和约束工作。

- **Agent Skills** — 支持按需加载可复用的技能说明、脚本和资源，为 Agent 增加特定领域的工作流程。

- **提示词模板** — 可以将常用提示词保存成 Markdown 模板，通过命令快速调用，并支持参数传递。

- **Extension 扩展系统** — 可以使用 TypeScript 扩展增加自定义工具、命令、快捷键、启动参数、事件处理和交互界面。

- **流程拦截与控制** — Extension 可以监听或拦截 Agent、模型、工具和会话事件，因此可以修改工具权限、审批流程、上下文内容和 Agent 执行逻辑。

- **自定义工具** — 可以为 Agent 注册新的工具，并定义工具参数、执行逻辑、结果展示和错误处理。

- **自定义模型供应商** — 除了通过配置文件添加兼容接口，还可以通过 Extension 实现新的认证方式、请求协议和流式响应格式。

- **终端界面定制** — 支持自定义状态栏、组件、对话框、编辑器、消息渲染和主题，可以在 Pi 终端界面上构建专属操作面板。

- **主题系统** — 支持自定义终端配色主题，并可通过配置或扩展切换界面风格。

- **Pi Packages** — 扩展、Skills、提示词模板和主题可以组合成可安装的软件包，通过 npm、Git 仓库或本地目录进行分发。

- **SDK 集成** — 可以把 Pi Agent 作为程序库嵌入其他 TypeScript/JavaScript 应用，由外部程序控制模型、工具、会话和事件。

- **RPC 与 JSON 模式** — 支持通过进程通信或 JSON 事件流调用 Pi，方便接入桌面应用、编辑器、自动化脚本和其他前端。

- **项目可信机制** — 可以区分可信和不可信项目，避免未知项目中的本地配置或扩展被直接加载。

- **外部隔离运行** — 可以配合 Docker、容器或受限系统环境运行，以隔离 Agent 执行的命令和文件操作。

- **跨平台运行** — 支持 Windows、Linux、macOS，并可在 Termux、tmux 等终端环境中使用。

## Pi 默认没有内置的能力

以下能力可以在 Pi200 中开发，但不能算作 Pi 当前的原生功能：

- 长期记忆和知识库
- 固定人格或角色养成系统
- 原生多 Agent 协作
- 定时任务和后台常驻任务
- 桌面 GUI
- 原生 MCP 客户端
- 每次命令执行前的权限确认弹窗
- 内置命令沙箱
- 内置 Plan 模式和 Todo 管理
- 后台持续运行的 Bash 任务

## 与 Pi200 的关系

Pi 的定位更接近一个极简但高度可扩展的 Agent 内核。Pi200 可以保留它现有的终端、模型、工具、会话和扩展系统，再逐步加入人格、记忆、权限、多 Agent 和桌面端等专属能力。
