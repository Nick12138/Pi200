# 建议 Pi200 开发功能

> 本文综合以下四份功能说明，并按照 Pi200 的实际定位重新整理：
>
> - `Pi功能说明.md`
> - `hanako功能说明.md`
> - `Hermes功能说明.md`
> - `Openclaw功能说明.md`
>
> Pi200 以 Pi 为底层，是一个 Windows 优先、本地优先的私人 AI 桌面应用。核心不是建设通用 Agent 平台，而是长期做好三个固定 Agent：文档处理助理、聊天对象和程序员。

## 产品定位

建议把 Pi200 定位为：

> **一个由文档处理助理、聊天对象和程序员组成的私人 AI 工作台。三个 Agent 共用 Pi 运行内核和桌面界面，但拥有不同的人格、工具、记忆、工作区和权限。**

三个 Agent 是产品的固定一级入口：

| Agent | 稳定 ID | 核心职责 | 默认权限 |
| --- | --- | --- | --- |
| 文档处理助理 | `document` | 阅读、整理、比较、提取、改写和生成文档 | 读取用户选择的文档，只写入输出目录 |
| 聊天对象 | `chat` | 日常聊天、陪伴、讨论、倾听和长期关系记忆 | 默认不能执行命令或访问工作区 |
| 程序员 | `coder` | 阅读代码、修改项目、执行命令、测试和使用 Git | 在授权项目中读写，危险操作需要审批 |

Agent 的显示名称、头像、人格和默认模型可以自定义，但内部 ID 应保持稳定，避免未来配置、会话和记忆迁移困难。

Pi200 不需要把这三个角色包装成一个无所不能的 Agent，也不需要早期开发任意数量的 Agent。用户应始终知道当前在和谁交流、它能访问什么，以及它准备执行什么操作。

## 产品原则

1. **固定三角色**：第一阶段只服务 `document`、`chat`、`coder`，不建设通用 Agent 创建器。
2. **显式选择**：用户通过桌面侧栏选择 Agent；第一版不做不可见的自动路由。
3. **默认隔离**：三个 Agent 的会话、长期记忆、工具权限和工作区默认隔离。
4. **产物驱动协作**：跨 Agent 协作传递任务说明、引用和输出文件，不直接共享完整隐私上下文。
5. **文档能力优先**：文档处理是 Pi200 最需要补足、也是最能形成产品差异的能力。
6. **复用 Pi 编程能力**：程序员 Agent 直接继承 Pi 已有工具、会话和 Extension 能力，不重复实现 Agent Loop。
7. **聊天低权限**：聊天对象首先是稳定、自然、可控的长期对话者，不默认拥有文件和命令权限。
8. **用户掌握记忆**：所有长期记忆都必须可查看、编辑、删除、关闭和导出。

## 四个参考项目对 Pi200 的价值

| 领域 | Pi | Hanako | Hermes Agent | OpenClaw | Pi200 选择 |
| --- | --- | --- | --- | --- | --- |
| Agent 内核 | 简洁、可修改、上游活跃 | 产品层改造较深 | Python 能力完整但体量较大 | Gateway 架构复杂 | 继续使用 Pi，并尽量保持上游兼容 |
| 程序员能力 | 文件、终端、Git、会话、Extension 很强 | 有桌面工作区 | 有远程环境和多 Agent | 有沙盒与设备节点 | 直接以 Pi 为程序员 Agent 底座 |
| 文档处理 | 有基础文件读取，Office 能力有限 | 有桌面预览和 Office 工具 | 通过 Skills 和工具扩展 | 通过插件处理 PDF 和文档 | 重点自建统一文档管线，复用成熟解析库 |
| 聊天与人格 | 可定制提示词 | 人格、Profile 和桌面聊天完整 | SOUL、Personality、Profile | SOUL、IDENTITY、USER 和多 Agent | 建立独立聊天人格与可控关系记忆 |
| 长期记忆 | 无内置长期记忆 | 分层记忆与事实库 | 有界记忆、FTS5、外部 Provider | 文件记忆和插件后端 | 先做有来源的 SQLite/FTS5 记忆，不急于上向量库 |
| 权限 | 无逐工具审批 | 会话权限和自动审核 | 命令与写入审批 | 多档 Exec 策略 | 为三个 Agent 分别设定默认权限和审批规则 |
| 桌面体验 | 无 GUI，但有 SDK/RPC | Electron 桌面较完整 | Electron 桌面端 | Web UI 和桌面 Hub | 桌面只做客户端，核心逻辑保留在 Pi200 Package |
| 多 Agent | 无内置 | Profile、Workflow、Subagent | Profile、Subagent、Kanban | 路由、绑定、Subagent | 只做固定三 Agent 和受控任务转交，不做开放式 Swarm |

综合结论：

- 从 Pi 获取可靠的 Agent Loop、模型接入、编程工具、会话和扩展系统。
- 从 Hanako 借鉴桌面交互、人格隔离、文件预览和私人助理体验。
- 从 Hermes 借鉴有界记忆、来源追踪、Skills 和任务执行记录。
- 从 OpenClaw 借鉴多 Profile 隔离、权限分层和可观测性，但不复制其庞大的渠道与设备体系。

## 整体结构

```text
Pi200 Desktop
├── 文档处理助理
│   ├── 文档工作台
│   ├── 文档解析与引用
│   └── 输出与版本比较
├── 聊天对象
│   ├── 人格与关系
│   ├── 私人记忆
│   └── 文本与语音聊天
├── 程序员
│   ├── 项目工作区
│   ├── 文件、终端与 Git
│   └── Diff、测试与任务记录
└── 共享底座
    ├── Pi Agent Runtime
    ├── Provider 与模型
    ├── 会话与上下文
    ├── 权限、审批与审计
    ├── SQLite、搜索与产物管理
    └── Agent 间任务转交
```

桌面端通过 Pi SDK 或 RPC 调用 Agent。Agent 流程、权限、记忆、文档处理和任务转交放在 `packages/Pi200`，不要在桌面前端复制一套 Agent Loop。

## Agent 一：文档处理助理

### 定位

文档处理助理是 Pi200 的主要生产力入口。它不仅要“读出文本”，还要理解文档结构、保留来源、展示页面、生成可交付文件，并让用户检查修改结果。

典型任务：

- 阅读合同、报告、论文、说明书、聊天记录和扫描件。
- 总结内容、提取重点、生成目录、问答和学习笔记。
- 比较两个版本，说明增加、删除和含义变化。
- 提取表格、字段、日期、金额、人员和待办事项。
- 翻译、润色、改写、纠错和统一格式。
- 根据模板生成报告、通知、方案、表格和演示文稿。
- 批量处理一个文件夹中的同类文档。

### 1. 文档接收与预览

建议按以下顺序支持格式：

1. 第一批：TXT、Markdown、HTML、PDF、DOCX、常见图片。
2. 第二批：XLSX、CSV、PPTX、邮件导出文件。
3. 第三批：扫描 PDF、复杂版式、旧版 Office 格式和压缩文档包。

需要具备：

- 文件选择、拖拽、剪贴板粘贴和文件夹批量导入。
- 最近文件、处理状态、失败原因和输出文件列表。
- PDF 分页预览、Office 转换预览、图片缩放和文本定位。
- 大文件分块处理，显示已处理页数和剩余进度。
- 文档指纹与缓存，避免同一文件反复解析。
- 原文件默认只读，输出进入独立目录。

不要把所有格式简单转换成一段纯文本。解析层应同时保留正文、标题层级、表格、页码、段落位置、批注、脚注和来源定位；版式预览与语义解析可以使用不同管线。

### 2. 阅读、问答与引用

- 对单个文档或一组文档进行摘要和问答。
- 回答必须能够引用文件名、页码、章节或表格位置。
- 点击引用后跳转到对应页面或段落。
- 区分“文档原文”“模型推断”和“外部资料”。
- 找不到证据时明确回答无法确认，不编造页码和引文。
- 支持全文搜索、关键词高亮、相关段落和引用回溯。
- 长文档先建立结构索引，再按问题读取相关部分，避免一次塞满上下文。

### 3. 提取与整理

- 按用户给出的字段提取结构化数据。
- 将多个文档汇总为表格、清单或时间线。
- 识别重复项、缺失项、矛盾项和异常值。
- 保存提取模板，供同类文件重复使用。
- 导出 CSV、XLSX、JSON 或 Markdown。
- 每个提取结果保留原始来源，允许回到文档复核。

第一版可以让模型生成候选结果，但涉及金额、日期、身份证明、合同条款等高风险字段时，必须提供逐项复核界面。

### 4. 比较、改写与生成

- 文档版本对比：文字变化、结构变化、表格变化和关键含义变化。
- 改写模式：润色、缩写、扩写、翻译、语气调整和术语统一。
- 模板生成：选择模板、填写变量、预览并导出。
- 输出前显示 Diff 或并排预览。
- 支持撤销、重新生成指定章节和保留用户手工修改。
- 生成 DOCX、PDF、Markdown、HTML；XLSX 和 PPTX 放到后续阶段。

第一版优先“读取原件，生成新文件”，不要直接覆盖原文件。真正的原位 Office 编辑应等到格式保真、撤销和备份机制稳定后再开放。

### 5. 文档库

文档库不是无限抓取的企业知识库，而是用户主动选择的本地资料集合：

- 文件夹级导入、移除和重新索引。
- 文件元数据、标签、摘要、全文索引和处理状态。
- SQLite FTS5 全文检索，后续再评估向量与混合检索。
- 文件改变后增量更新，不重复处理未变化文件。
- 删除文件或移除资料库后，同步删除索引和缓存。
- 支持按项目建立多个互相隔离的文档工作区。

### 6. 文档 Agent 的第一版边界

第一版必须完成：

- PDF、DOCX、TXT、Markdown 和图片导入与预览。
- 扫描图片的基础 OCR。
- 摘要、问答、引用定位和全文搜索。
- 翻译、润色、结构化提取和 Markdown/DOCX 导出。
- 原文件只读、输出目录、处理进度和失败提示。

第一版暂不承诺复杂 PDF 原位编辑、完美保留所有 Word 排版、Excel 公式编辑、PPT 动画或大型企业知识库。

## Agent 二：聊天对象

### 定位

聊天对象负责自然、连续、有性格的私人交流。它可以陪伴、倾听、讨论想法和帮助梳理情绪，但不应默认表现成工作流工具，也不应在用户不知情时访问文档或代码。

### 1. 人格与关系

- 独立的 `identity.md`、`soul.md`、`user.md` 和 `instructions.md`。
- 用户可以设置名称、头像、声音、语气、称呼和交流边界。
- 人格修改提供预览和版本记录，可以恢复上一个版本。
- 保持表达一致，但允许在不同聊天场景中切换轻松、认真或安静模式。
- 默认回复更自然简洁，不把普通聊天写成报告，也不展示无关工具过程。

### 2. 聊天记忆

聊天对象需要记住关系中真正有价值的信息，但记忆必须有界：

- 用户资料：称呼、偏好、重要人物和沟通习惯。
- 长期事实：用户明确要求记住的事情。
- 关系记忆：有意义的共同话题、约定和阶段性经历。
- 会话摘要：长会话结束或压缩时生成。
- 临时状态：近期话题和情绪线索，按时间自动淡出。

每条长期记忆保存来源、创建时间、最后使用时间和适用 Agent。用户可以查看、修改、删除、固定或禁止某类内容进入记忆。敏感内容默认不自动保存。

### 3. 日常聊天体验

- 会话搜索、置顶、归档、分支和继续聊天。
- 文本、图片、文件片段和语音消息。
- 可选的语音转写与 TTS 回复。
- 回复速度和推理强度可独立配置，不必所有聊天都使用最高推理模型。
- 支持“只听我说”“帮我分析”“一起想办法”等明确对话意图。
- 用户可以引用某次历史聊天继续讨论。
- 断网、模型失败或上下文压缩时给出清晰状态，不伪造已经记住。

### 4. 主动能力

主动能力应在聊天稳定后逐步增加：

- 用户设置的一次性提醒。
- 生日、纪念日和明确事件提醒。
- 可配置的每日问候或定期回顾。
- 桌面通知和免打扰时段。

默认不启用无限 Heartbeat，不允许聊天对象自行决定长期后台目标。所有主动行为都要能在设置页查看来源、频率、下次执行时间和关闭入口。

### 5. 聊天 Agent 的权限边界

- 默认没有 Shell、Git 和工作区写权限。
- 只能读取用户主动发送的附件。
- 需要查网页时单独申请网络工具权限。
- 不能自动读取文档 Agent 的资料库或程序员 Agent 的代码。
- 向其他 Agent 转交内容前显示将要共享的文本、文件和记忆。
- 不把“关系维护”作为诱导用户持续使用或阻止用户离开的理由。

### 6. 聊天 Agent 的第一版边界

第一版必须完成：

- 稳定人格、独立会话和可编辑用户资料。
- 有来源、可关闭的基础长期记忆。
- 文本与图片聊天、会话搜索和上下文压缩。
- 快速停止、删除会话和彻底删除指定记忆。

语音、主动提醒和更复杂的情绪理解放在第二阶段。

## Agent 三：程序员

### 定位

程序员 Agent 是 Pi 现有 Coding Agent 能力的桌面化和产品化版本。它需要忠实继承 Pi 的文件、终端、模型、会话和 Extension 能力，并补上权限审批、工作区管理、Diff 预览和任务恢复。

### 1. 项目工作区

- 添加、打开和移除本地 Git 仓库或普通代码目录。
- 每个会话绑定一个主工作目录。
- 显示文件树、Git 状态、当前分支和最近提交。
- 额外目录默认只读，写入需要用户授权。
- 记录项目级 Agent 指令、常用命令和环境诊断。
- 支持多个项目，但一个会话只允许一个主要可写工作区。

### 2. 编程任务流程

- 阅读、搜索和解释代码。
- 修改、新建和删除文件。
- 执行格式化、检查、测试和用户批准的开发命令。
- 显示计划、进行中的步骤、工具状态和最终结果。
- 修改前后展示 Diff，支持逐文件接受和恢复。
- 失败时保留命令输出、退出码和可重试入口。
- 中止任务时终止当前命令和子任务。

第一版可以继续使用 Pi 的现有 Agent Loop。只有当三个 Agent 的路由、权限或上下文流程无法通过 Extension 和 SDK 实现时，才小范围修改 Pi 核心源码。

### 3. Git 与安全

- 提交前显示文件列表和 Diff，不默认 `git add .`。
- 危险 Git 命令、强制推送、删除分支和覆盖远程状态必须单独确认。
- 密钥、Token、`.env` 和凭据文件提供提交前扫描。
- 命令权限分为只读、询问、自动审核和完整权限。
- 保存工具审批与命令审计记录。
- AI 修改前创建短期文件快照，允许恢复。

### 4. 程序员记忆

程序员 Agent 的长期状态主要属于项目，而不是聊天关系：

- 项目架构、运行方式、检查命令和发布流程。
- 用户确认的代码风格与工程约定。
- 已完成任务摘要和重要技术决策。
- 常见故障及经过验证的解决方法。

优先读取仓库中的 `AGENTS.md` 和项目文档。Agent 记忆不能覆盖仓库内最新规则；发生冲突时，以当前项目文件为准并提示用户。

### 5. 后续增强

- Git Worktree 和隔离任务分支。
- 代码符号索引与更精确的上下文选择。
- 浏览器文档检索和本地 API 文档索引。
- 临时代码审查、测试或研究 Subagent。
- Docker 开发环境和远程 SSH 工作区。
- 可复用的项目工作流模板。

这些能力在单 Agent 编程流程稳定后再增加，不在第一版同时建设复杂 Workflow、Kanban 和多层 Subagent。

### 6. 程序员 Agent 的第一版边界

第一版必须完成：

- 继承 Pi 的文件、搜索、终端、图片、模型和会话能力。
- 桌面工作区、文件树、Git 状态、命令输出和 Diff 预览。
- 权限审批、中止、错误展示和恢复入口。
- 项目级指令与会话恢复。

## 三个 Agent 如何协作

### 第一版：用户显式转交

桌面端提供明确命令：

- `交给文档处理助理`
- `交给聊天对象`
- `交给程序员`

转交前展示：

- 任务说明。
- 将共享的消息范围。
- 将共享的文件和输出产物。
- 目标 Agent 将获得的临时权限。

用户确认后，目标 Agent 创建独立会话。原 Agent 只收到目标 Agent 的最终结果、引用和产物链接，不自动获得目标 Agent 的全部会话和长期记忆。

典型协作：

- 文档处理助理发现需要批量转换脚本，经用户同意后交给程序员编写。
- 程序员把产品需求文档交给文档处理助理提取验收条件，再返回当前代码会话。
- 用户把一段工作结果主动分享给聊天对象讨论，但聊天对象不会自动读取整个项目。

### 后续：透明的协作建议

后续可以让当前 Agent 建议转交，但不能静默执行。例如文档 Agent 可以提示“这项任务需要编写脚本，是否交给程序员？”用户仍然决定是否共享内容和启动另一个 Agent。

不建议早期加入第四个“总管 Agent”。任务选择、路由和权限确认先由桌面应用的确定性逻辑完成，避免隐藏成本、错误路由和权限扩大。

## 共享数据与隔离

### 共享内容

三个 Agent 可以共享：

- Provider、模型目录和代理配置。
- 用户主动填写的基础资料和通用语言偏好。
- 主题、快捷键和桌面设置。
- 用户明确标记为“所有 Agent 可用”的文件或事实。
- 经用户确认的 Agent 间任务转交记录。

### 默认隔离内容

| 数据 | 文档处理助理 | 聊天对象 | 程序员 |
| --- | --- | --- | --- |
| 会话历史 | 独立 | 独立 | 独立 |
| 长期记忆 | 文档工作方式与资料库 | 关系和个人偏好 | 项目规则与技术决策 |
| 工作区 | 文档输入与输出目录 | 无默认工作区 | 授权代码项目 |
| 工具 | 文档解析、OCR、导出 | 聊天、可选语音 | 文件、Shell、Git、测试 |
| 默认写入 | 只写输出目录 | 不写文件 | 只写授权项目 |

用户可以临时共享，但不应存在“所有 Agent 自动读取所有内容”的总开关。即使用户选择共享，也要显示数据范围和撤销入口。

## 权限、审批与审计

权限系统必须早于自动化、浏览器控制和 Subagent。

建议保留四档权限：

| 模式 | 行为 |
| --- | --- |
| 只读 | 只允许读取、搜索和分析 |
| 操作前询问 | 写入、命令、网络提交和外部操作逐次确认 |
| 自动审核 | 低风险操作自动通过，高风险或无法判断时询问 |
| 完整权限 | 在明确授权范围内执行已启用工具，不逐次询问 |

同时实现：

- 工具声明读写范围、网络访问和副作用类型。
- 文件与命令通过同一个权限判定入口。
- 权限按 Agent、会话和工作区分别保存。
- 临时授权在会话结束后失效。
- 每次拒绝向 Agent 返回明确原因和可行下一步。
- 审计日志记录 Agent、会话、工具、目标、审批人、时间和结果。
- 桌面端始终提供停止当前 Agent 和全部后台任务的入口。

## Provider 与模型策略

三个 Agent 可以使用同一个 Provider，但应支持分别设置默认模型：

- 文档处理助理：优先长上下文、视觉理解、结构化输出和稳定引用。
- 聊天对象：优先自然表达、响应速度、价格和可控人格。
- 程序员：优先代码能力、工具调用、推理和长任务稳定性。

每个会话允许临时切换模型和推理强度。模型配置页需要显示上下文长度、图像支持、工具支持和预计费用，不能只显示模型名称。

## 数据目录与模型

Pi200 不应长期与原版 Pi 共用 `~/.pi/agent`。建议默认使用：

```text
%USERPROFILE%\.pi200\
├── config\                     # Provider、模型、代理和桌面设置
├── agents\
│   ├── document\               # 文档 Agent 人格、记忆和会话索引
│   ├── chat\                   # 聊天 Agent 人格、记忆和会话索引
│   └── coder\                  # 程序员人格、记忆和会话索引
├── workspaces\
│   ├── documents\              # 文档工作区定义
│   └── coding\                 # 代码工作区定义，不复制原项目
├── artifacts\                  # 三个 Agent 的输出文件
├── cache\                      # 可安全重建的预览、OCR 和索引缓存
├── logs\                       # 诊断与审计日志
└── pi200.db                     # 会话索引、记忆、权限和任务状态
```

建议使用 SQLite 保存结构化状态，FTS5 用于会话、记忆和文档全文搜索。Markdown 只用于用户需要直接编辑和迁移的人格、规则及记忆摘要。凭据必须使用独立敏感文件或系统凭据存储，不能进入公开 Git 仓库。

所有数据结构从第一版就包含稳定的 `agentId`、`sessionId`、`workspaceId` 和 `artifactId`。跨 Agent 转交使用明确的 `handoffId`，记录共享范围和用户批准信息。

## 桌面端建议

桌面端第一版使用统一窗口，不为三个 Agent 各做一套应用：

- 左侧：三个固定 Agent、各自会话列表和未完成任务。
- 中间：当前对话、流式回复、工具状态和输入区。
- 右侧：根据 Agent 切换文档预览、记忆详情、文件 Diff 或终端输出。
- 顶部：当前 Agent、模型、推理强度、工作区和权限模式。
- 底部或状态区：代理、Token 使用、后台任务和错误状态。

三个 Agent 应共享基础消息组件，但展示重点不同：

- 文档处理助理突出页面、引用、提取表格和输出文件。
- 聊天对象隐藏不必要的技术信息，突出自然对话和记忆控制。
- 程序员突出工具调用、命令输出、Git 状态和 Diff。

## 开发阶段

### P0：三 Agent 共同底座

- `Pi200` 独立数据目录和从 Pi 导入配置的迁移流程。
- 稳定的 `document`、`chat`、`coder` Profile 定义。
- 会话、工作区、记忆、产物和权限的 SQLite 数据模型。
- Agent 隔离、权限审批、审计和统一停止机制。
- Pi 上游同步流程和最小回归检查。
- 桌面端与 Pi Runtime 的 RPC 通信。

### P1：首个可用版本

文档处理助理：

- PDF、DOCX、TXT、Markdown、图片读取与预览。
- 摘要、问答、引用、翻译、润色和基础提取。
- Markdown/DOCX 输出，原文件只读。

聊天对象：

- 可定制人格、独立会话、用户资料和基础长期记忆。
- 文本与图片聊天、会话搜索、删除和上下文压缩。

程序员：

- 接入 Pi 现有编程能力。
- 桌面工作区、文件树、Git 状态、命令输出和 Diff。
- 操作审批、中止和任务结果说明。

共同能力：

- 三 Agent 显式切换。
- 各自默认模型与权限。
- 输出产物中心。
- 基础备份、导入、导出和诊断页。

### P2：强化三个核心 Agent

文档处理助理：

- OCR、XLSX、CSV、PPTX、批量处理和版本比较。
- 提取模板、文档工作区、FTS5 检索和引用跳转。
- 更可靠的 DOCX/PDF 生成与并排预览。

聊天对象：

- 语音转写、TTS、免打扰和用户设置的提醒。
- 关系记忆、记忆冲突提示和人格版本管理。

程序员：

- 文件快照、恢复、测试结果视图和提交前密钥扫描。
- Worktree、项目知识索引和浏览器文档检索。

共同能力：

- 用户确认的跨 Agent 任务转交。
- 更细粒度的工具授权和审计查询。
- Agent、记忆、会话和产物的完整导入导出。

### P3：按真实需求扩展

- 文档工作流模板和高质量 Office 编辑。
- 程序员临时 Subagent 和隔离开发环境。
- MCP Client，并按 Agent 启用具体工具。
- 用户明确配置的 Cron、提醒和桌面通知。
- 响应式 Web/PWA，用于远程继续会话和查看任务。

### P4：明确需要后再开发

- Telegram、飞书等消息渠道。
- 通用插件 SDK 和第三方插件生态。
- Docker、SSH 和云执行后端。
- 高级向量检索和自动学习建议。
- 设备节点、电脑控制和原生移动端。

## 推荐开发优先级

| 优先级 | Agent | 功能 | 主要价值 | 主要风险 |
| --- | --- | --- | --- | --- |
| P0 | 共享 | 独立数据目录、三 Profile、存储迁移 | 产品基础 | 数据兼容 |
| P0 | 共享 | 权限、审批、停止、审计 | 安全基础 | 权限绕过 |
| P0 | 共享 | Pi 上游同步与回归检查 | 长期维护 | 合并冲突 |
| P1 | 共享 | 三 Agent 桌面切换与会话隔离 | 核心体验 | 状态串线 |
| P1 | 文档 | PDF/DOCX/图片解析、预览、问答和引用 | 主要差异化 | 解析准确性 |
| P1 | 文档 | 改写、提取和 DOCX/Markdown 输出 | 可交付结果 | 格式丢失 |
| P1 | 聊天 | 人格、用户资料和可控记忆 | 长期使用价值 | 错误或敏感记忆 |
| P1 | 程序员 | Pi 工具、工作区、Diff 和命令视图 | 立即可用 | 文件副作用 |
| P2 | 文档 | OCR、表格、PPTX、批处理和版本比较 | 深度文档能力 | 版式复杂度 |
| P2 | 聊天 | STT、TTS、提醒和关系记忆 | 陪伴体验 | 隐私与主动打扰 |
| P2 | 程序员 | 快照恢复、Worktree、项目索引 | 开发可靠性 | Git 状态管理 |
| P2 | 共享 | 跨 Agent 任务转交 | 三角色协作 | 隐私泄漏 |
| P3 | 共享 | MCP、受控自动化和 PWA | 扩展能力 | 后台副作用 |
| P3 | 程序员 | 临时 Subagent 与容器 | 复杂任务能力 | 成本与并发 |
| P4 | 共享 | 多渠道、设备节点、开放插件生态 | 远程与生态 | 维护复杂度 |

## 不建议早期开发的功能

- 任意创建大量 Agent、Agent 社交系统或大规模 Swarm。
- 一个隐藏 Agent 自动决定所有任务路由。
- 三个 Agent 默认共享全部会话、文件和长期记忆。
- 聊天对象默认拥有 Shell、Git 或整个磁盘的读取权限。
- 文档处理没有预览和备份就直接覆盖原文件。
- 同时追求所有 Office 格式的完美原位编辑。
- 没有引用来源的文档问答和结构化提取。
- 没有审计与停止入口的 Cron、Heartbeat 和后台任务。
- 同时接入所有国内外消息平台。
- 原生 iOS、Android、watchOS 和设备节点同时开工。
- 在基础搜索准确前自建复杂向量数据库集群。
- 默认开启人格、记忆、Skills 或代码的自动自我修改。
- 为未知未来需求提前设计庞大的插件 API。
- 在桌面前端重新实现一套 Agent Loop。

## 推荐代码目录

```text
Pi200/
├── apps/
│   └── desktop/                 # 三 Agent 共用的桌面客户端
├── packages/
│   └── Pi200/
│       ├── src/
│       │   ├── agents/
│       │   │   ├── document/    # 文档 Agent 流程与提示词
│       │   │   ├── chat/        # 聊天 Agent 人格与流程
│       │   │   └── coder/       # Pi Coding Agent 适配
│       │   ├── documents/
│       │   │   ├── parsers/     # 格式解析适配器
│       │   │   ├── renderers/   # 页面与文件预览
│       │   │   ├── extraction/  # 引用、字段和表格提取
│       │   │   └── exporters/   # DOCX、PDF、Markdown 等输出
│       │   ├── coordination/     # 显式任务转交与产物传递
│       │   ├── memory/           # 分 Agent 记忆、摘要和检索
│       │   ├── permissions/      # 权限、审批和审计
│       │   ├── storage/          # SQLite、缓存和迁移
│       │   ├── artifacts/        # 输出文件登记与版本
│       │   └── config/           # 产品、Agent、模型和代理配置
│       └── test/
├── scripts/                     # 启动、上游同步、诊断和发布
└── Pi 原有 packages/            # 尽量保持与 upstream 一致
```

当前不要一次创建全部空目录。先围绕第一个真实功能纵向实现，例如先打通“导入 PDF -> 解析 -> 带引用问答 -> 预览定位 -> 导出结果”，再抽取文档适配接口。

## 首个公开版本的验收标准

首个版本不以功能数量验收，而以三个 Agent 都能完成一条真实闭环为准。

### 文档处理助理

- 导入一份 PDF 或 DOCX，正确显示处理进度和预览。
- 根据文档回答问题，并能跳转到实际引用位置。
- 完成一次润色或结构化提取，导出一个新文件。
- 原文件未被覆盖，失败时有明确原因。

### 聊天对象

- 人格在多次会话中保持基本一致。
- 用户可以明确要求记住一件事，并在记忆页查看和删除。
- 删除后新会话不再检索到该记忆。
- 普通聊天不触发文件、Shell 或 Git 工具。

### 程序员

- 打开一个项目，读取规则并完成一次受控代码修改。
- 用户能够查看 Diff、命令输出和检查结果。
- 拒绝权限或中止任务后，Agent 正确停止并说明状态。
- 会话恢复后能继续理解项目和当前任务。

### 共同体验

- 三个 Agent 的会话、记忆和权限不会串线。
- 用户能看见当前 Agent、模型、工作区和权限。
- 跨 Agent 共享前必须确认具体消息与文件。
- 所有输出文件都能从产物中心找到和打开。
- 配置、人格、记忆和会话可以备份与迁移。

## 最重要的工程原则

- **先把三个 Agent 做深**：不以 Agent 数量、渠道数量或插件数量作为进度。
- **文档结果必须可核对**：引用、来源、预览和输出文件比一段看似正确的回答更重要。
- **聊天记忆必须可控**：不保存不代表遗忘失败，用户明确关闭和删除应被严格执行。
- **程序员操作必须可恢复**：Diff、审批、中止、快照和 Git 状态是核心体验。
- **隔离优先于便利**：跨 Agent 数据共享必须显式发生并可撤销。
- **上游优先**：Pi 已有能力不重写，Pi 核心修改保持小而有测试。
- **成熟库优先**：文档格式、OCR、Git 和协议处理使用可靠库，不手写脆弱解析器。
- **失败必须可见**：每项处理都有等待、运行、成功、失败、拒绝或取消状态。
- **桌面只是客户端**：核心行为放在 Pi200 Package，避免逻辑散落到 UI。
- **每个阶段都有完整闭环**：先完成可真实使用的纵向流程，再扩展格式、工具和平台。
