Pi 官方文档：https://pi.dev/docs/latest

# Pi200 项目路径说明

本文件记录 Pi200 项目的重要路径、配置位置和维护备注。新增路径或约定时，继续补充到这里。

## 项目目录

| 路径 | 用途 |
| --- | --- |
| `D:\我的项目\Pi200` | Git 仓库根目录；在这里执行 Git、安装、运行和检查命令。 |
| `D:\我的项目\Pi200\packages\Pi200` | Pi200 Agent 的专属逻辑、工具、流程和策略。 |
| `D:\我的项目\Pi200\apps\desktop` | Pi200 桌面应用；技术栈和实际代码后续确定。 |

## Pi 用户配置

这些文件位于公开 Git 仓库之外，不会随项目上传 GitHub。

| 路径 | 用途 | 备注 |
| --- | --- | --- |
| `C:\Users\liu\.pi\agent\models.json` | 自定义供应商和模型定义。 | 可以记录地址和模型信息，不应直接保存到公开仓库。 |
| `C:\Users\liu\.pi\agent\auth.json` | API Key 和 OAuth 凭据。 | 敏感文件；禁止复制到项目或提交 Git。 |
| `C:\Users\liu\.pi\agent\settings.json` | 默认供应商、模型、推理强度和界面设置。 | 修改后建议重新启动 Pi。 |
| `C:\Users\liu\AppData\Roaming\npm\pi200.cmd` | 全局 `pi200` 命令入口。 | 位于现有用户 `PATH` 中，从任意目录启动 Pi200。 |
| `C:\Users\liu\AppData\Roaming\npm\pi200-launch.ps1` | 全局命令内部包装器。 | 读取 `PI200_HOME` 并调用项目启动脚本。 |
| 用户环境变量 `PI200_HOME` | Pi200 项目根目录。 | 当前值为 `D:\我的项目\Pi200`，供全局命令定位中文路径。 |

## 当前自定义供应商

- 显示名称：`Ningyi`
- 内部 ID：`ningyi`
- API 地址：`https://ningyiai.siphot.com/v1`
- API 协议：`openai-completions`
- 默认模型：`gpt-5.6-sol`
- 默认推理强度：`xhigh`
- 密钥位置：`C:\Users\liu\.pi\agent\auth.json`（密钥值不记录在本文档中）

## 启动 Pi200

在任意终端目录执行全局命令：

```powershell
pi200
```

全局命令会调用项目启动脚本，并支持继续传递参数，例如：

```powershell
pi200 --version
```

也可以在仓库根目录通过 npm 启动：

```powershell
cd "D:\我的项目\Pi200"
npm run pi200
```

检查版本但不进入交互界面：

```powershell
npm run pi200 -- --version
```

启动脚本：`D:\我的项目\Pi200\scripts\start-pi200.ps1`

启动脚本会自动执行以下准备工作：

- 继承已有代理环境变量；未设置时读取 Windows 系统代理。
- 缺少依赖时执行 `npm install --ignore-scripts`。
- 检查 Pi 模型数据，缺失或过期时执行 `npm run hydrate:model-data`。
- 使用 Git Bash 启动当前仓库中的源码版 Pi。

## 维护约定

- 任何密钥、Token、密码或真实 `.env` 文件都不能写入本仓库。
- 项目路径、运行入口、外部配置位置和重要操作约定发生变化时，更新本文件。
- 运行源码版 Pi 时，从 `D:\我的项目\Pi200` 执行 `npm run pi200`，不从子 package 目录启动。
